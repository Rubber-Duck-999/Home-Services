name: Production Actions

on:
  pull_request:
    types: closed
    branches: [ "main" ]

jobs:
  deploy:
    runs-on: [self-hosted, linux, ARM64]
    steps:
    - uses: actions/checkout@v3
    - name: Deploy to network node
      run: scp tesseract.py constants.py pi@192.168.0.6:/home/pi/deploy/
